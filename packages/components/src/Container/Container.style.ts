import styled from 'styled-components'

export const StyledContainer = styled.div`
  height: 100%;
  display: grid;
  padding-top: 4rem;
  grid-template-columns: repeat(2, 1fr);
  grid-gap: 15px;
  justify-content: center;
  align-items: center;
  background: rgb(54,46,181);
  background: linear-gradient(180deg, rgba(54,46,181,1) 0%, rgba(103,114,229,1) 36%, rgba(200,221,226,0) 100%);

`
